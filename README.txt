// the following java files were created:
//
// the BrowserNavigation file is used to test the implementation
// of the class which will manage the history of weblinks the user navigates to
//
// The StackList.java file manages items in a singly linked list using push()
// and pop() to control what item is added or removed from the stack
//
// the Navigator.java file provides the navigation feature of BrowserNavigation
// and allows the user to set the current link, or go back and forward one link
//
// the run file displays the output
//
// the links.txt file displays that links that the user will navigate through

